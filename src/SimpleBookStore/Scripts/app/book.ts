export class Book {
    constructor(
        public id: number,
        public title: string,
        public author: string,
        public genre: number,
        public publisher: string,
        public year: number
    ) {

    }
}